import { InputErrorsComponent } from './input-errors.component';

describe('InputErrorsComponent', () => {
  let component: InputErrorsComponent;

  beforeEach(() => {
    component = new InputErrorsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
