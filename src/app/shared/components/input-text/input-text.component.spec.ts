import { InputTextComponent } from './input-text.component';

describe('InputTextComponent', () => {
  let component: InputTextComponent;

  beforeEach(() => {
    component = new InputTextComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
